@model IEnumerable<Models.Allocation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Booking.studentNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.residenceCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.roomId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.blockCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Booking.studentNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.residenceCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.roomId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoomType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.blockCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllocationRefNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllocationRefNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllocationRefNo })
        </td>
    </tr>
}

</table>
