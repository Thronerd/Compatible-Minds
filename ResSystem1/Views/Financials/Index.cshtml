@model IEnumerable<Models.Financial>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.onlinepayment.cardType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.funder)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.onlinepayment.cardType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.funder)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.funderId }) |
                @Html.ActionLink("Details", "Details", new { id=item.funderId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.funderId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
